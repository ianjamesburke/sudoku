schema_version = 1

[app]
id = "sudoku"
type = "app"
name = "Sudoku"
entry = "main.py"
version = "0.1.0"
min_sdk_version = "0.1.13"
description = "Sudoku with easy, medium, and hard difficulties. Click or use arrow keys to select cells, type 1-9 to fill, N for notes mode."
watch = true

[app.capabilities]
capabilities = ["timer"]

[launch]
